# name: Helm Chart CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   lint-test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Helm
#         uses: azure/setup-helm@v4
#         with:
#           version: '3.14.0'

#       - name: Lint Helm chart
#         run: |
#           helm lint ./charts

#       - name: Template Helm chart
#         run: |
#           helm template test-release ./charts > /dev/null
#           echo "✅ Chart templates rendered successfully"

#       - name: Template with dev values
#         run: |
#           helm template test-release ./charts -f values-dev.yaml > /dev/null
#           echo "✅ Dev values validated"

#       - name: Template with prod values
#         run: |
#           helm template test-release ./charts -f values-prod.yaml > /dev/null
#           echo "✅ Prod values validated"